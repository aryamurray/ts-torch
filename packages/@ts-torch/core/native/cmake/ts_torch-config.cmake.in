@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find PyTorch dependency
find_dependency(Torch REQUIRED)

# Include targets
if(NOT TARGET ts_torch::ts_torch)
    include("${CMAKE_CURRENT_LIST_DIR}/ts_torch-targets.cmake")
endif()

check_required_components(ts_torch)
